import { defineProblem } from "../../core/schemas";

export default defineProblem({
  id: "learn-bash-quoting",
  stage: "learn",
  language: "bash",
  scenario: "sh",
  copy: {
    title: "クォート（' と \"）の違いを知る",
    prompt: `## bash には3つの「囲み方」がある

| 書き方 | 変数展開 | スペースでの分割 | 基本の使い分け |
|---|---|---|---|
| \`"$var"\` | される | されない | **基本はコレ**。迷ったらダブルクォート |
| \`'$var'\` | されない（文字通り） | されない | 完全に固定した文字列を書きたいとき |
| \`$var\`（クォートなし） | される | **される（危険）** | 基本使わない |

## シングルクォートは「そのまま」

\`\`\`bash
echo '$HOME'   # → $HOME （変数展開されない、文字通り）
echo "$HOME"   # → /home/xxx （ダブルクォートなら展開される）
\`\`\`

## クォートなしは危険

\`\`\`bash
name="Alice Bob"
echo $name     # クォートなし。スペースで単語分割される可能性がある
echo "$name"   # ダブルクォートで囲めば1つの値として安全に扱える
\`\`\`

**迷ったら \`"$var"\` のようにダブルクォートで囲む**のが基本。`,
    hints: [
      "シングルクォート `'...'` の中は変数展開されない（文字通り）。",
      "ダブルクォート `\"...\"` の中は変数展開される。",
      "クォートなしの変数展開はスペースで分割されることがあるので基本使わない。",
    ],
  },
  learn: {
    quiz: [
      {
        prompt: "`echo '$HOME'` の出力は？",
        snippet: `echo '$HOME'`,
        choices: ["$HOME（文字通り）", "実際のホームディレクトリのパス", "エラーになる"],
        answer: "$HOME（文字通り）",
        explain:
          "シングルクォートの中は変数展開されない。`$HOME` という文字列がそのまま出力される。",
      },
      {
        prompt: "`echo \"$HOME\"` の出力は？",
        snippet: `echo "$HOME"`,
        choices: ["実際のホームディレクトリのパス", "$HOME という文字列", "何も表示されない"],
        answer: "実際のホームディレクトリのパス",
        explain: "ダブルクォートの中は変数展開される。`$HOME` の中身（パス）が出力される。",
      },
      {
        prompt:
          '`name="Alice Bob"` としたあと `echo $name`（クォートなし）を実行すると起こりうることは？',
        choices: [
          "スペースで単語分割されて意図しない結果になることがある",
          "必ずエラーになる",
          "何も変わらず常に安全",
        ],
        answer: "スペースで単語分割されて意図しない結果になることがある",
        explain:
          "クォートなしの変数展開はスペースなどで単語分割（さらに glob 展開）される。基本は `\"$var\"` のように囲むのが安全。",
      },
      {
        prompt: "変数を安全に展開したいとき、基本どちらを使うべき？",
        choices: ['"$var"（ダブルクォート）', "$var（クォートなし）"],
        answer: '"$var"（ダブルクォート）',
        explain: "基本は `\"$var\"` 一択。単語分割や glob 展開を防げる。",
      },
      {
        prompt: "シングルクォート `'...'` の特徴として正しいのは？",
        choices: [
          "中に変数を書いても展開されない",
          "変数展開だけがされる",
          "コマンド置換だけがされる",
        ],
        answer: "中に変数を書いても展開されない",
        explain: "シングルクォートの中は一切展開されない。書いた文字がそのまま値になる。",
      },
    ],
  },
});

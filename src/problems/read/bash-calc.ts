import { defineProblem } from "../../core/schemas";

export default defineProblem({
  id: "read-bash-calc",
  stage: "read",
  language: "bash",
  scenario: "sh",
  copy: {
    title: "数字を変えて計算結果を変える",
    prompt: `## まず実行してみよう

**▶ 実行** を押すと、計算結果が表示される。

次に、コードの \`2\` や \`3\` を好きな数字に変えてもう一度実行しよう。
結果が変わるはず。

## コードを読んでみよう

\`\`\`bash
echo $((2 + 3)) # ← ここの数字を変える
\`\`\`

- \`$(( ))\` — 算術式（計算）を評価して、その**値**を返す
- \`(( ))\`（\`$\` なし）は真偽の判定、\`$(( ))\`（\`$\` あり）は値そのものを返す点が違う

**数字（\`2\` \`3\`）だけ変えれば OK。**`,
    hints: [
      "`2` や `3` を好きな数字に変えてみよう",
      "`+` を `-` や `*` に変えても遊べる",
      "`$(( ))` は計算結果を値として返す",
    ],
  },
  initialCode: `echo $((2 + 3)) # ← ここの数字を変えて ▶ 実行！`,
  solutionCode: `echo $((2 + 3))`,
});
